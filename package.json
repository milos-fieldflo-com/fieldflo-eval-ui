{
  "name": "fieldflo-evals-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n fe,be -c cyan,green \"vite\" \"cd backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 3101 --reload\"",
    "dev:fe": "vite",
    "dev:be": "cd backend && uv run uvicorn app.main:app --host 127.0.0.1 --port 3101 --reload",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "concurrently": "^9.2.1",
    "typescript": "~5.7.0",
    "vite": "^6.0.0"
  }
}
